import { AuthCardFooter } from "./auth-card-footer";
import { AuthCardHeader } from "./auth-card-header";
import { AuthCardRoot } from "./auth-card-root";

export const AuthCard = {
    Root: AuthCardRoot,
    Header: AuthCardHeader,
    Footer: AuthCardFooter,
};
