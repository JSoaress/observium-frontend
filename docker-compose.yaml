services:
    observium-web:
        command: sh -c "yarn dev"
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        env_file: ./.env
